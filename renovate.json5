{
  extends: ['github>valora-inc/renovate-config:default.json5'],
  ignoreDeps: [
    'node', // ignore node, which is forced to v16
    'chai', // ignore chai, which 5.x breaks tests
    'chai-as-promised',
  ],
}
